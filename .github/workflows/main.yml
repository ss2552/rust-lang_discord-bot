name: Rust

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:

    runs-on: alpine-latest

    steps:
    - uses: actions/checkout@v4
    - name: deploy
      run: \
        - ls
        - cargo build
